{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "bunVersion": "1.x",
  "installCommand": "corepack enable && corepack prepare pnpm@11.1.1 --activate && pnpm install --frozen-lockfile",
  "buildCommand": "pnpm run build",
  "outputDirectory": "dist",
  "functions": {
    "api/hello.ts": {
      "includeFiles": "../../packages/handler/src/index.ts"
    }
  }
}
